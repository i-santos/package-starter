---
"@i-santos/create-package-starter": patch
---

Refine `init` behavior to append template entries into existing `.gitignore`, keep `README.md` and `CONTRIBUTING.md` create-only, align PR template content, and run `npm install` at the end of initialization.
