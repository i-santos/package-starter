---
'@i-santos/ship': patch
---

Fail fast in `ship release-cycle` when the release workflow has already completed with failure, and include workflow run details plus failed log excerpt instead of waiting for npm propagation timeout.
