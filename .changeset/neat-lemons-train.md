---
"@i-santos/ship": major
"@i-santos/npmstack": major
---
Rename the ship orchestration command from `release-cycle` to `release` and update npmstack ship passthrough/docs/tests accordingly.

Migration:
- Replace `ship release-cycle ...` with `ship release ...`
- Replace `npmstack ship release-cycle ...` with `npmstack ship release ...`
