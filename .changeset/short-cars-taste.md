---
'@i-santos/ship': minor
---

Publish `@i-santos/ship`, a reusable release-cycle orchestration engine with `ship open-pr` and `ship release-cycle` commands and optional `.ship.json` project config.
