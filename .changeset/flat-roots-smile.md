---
"@i-santos/ship": patch
---
Flatten repository layout by moving `ship` package files from `packages/ship` to the repository root (`bin`, `lib`, `template`, and package metadata).

No CLI command changes; this is an internal packaging/layout migration.
