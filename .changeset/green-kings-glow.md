---
"@i-santos/create-package-starter": minor
---

Add an auto-retarget pull request workflow template that enforces default base routing (`feat|fix|chore -> release/beta`, `release/beta -> main`) and include it in create/init managed assets.
