---
'@i-santos/ship': minor
---

Unify beta provisioning into `ship setup-github` so GitHub baseline + beta branch/ruleset + local beta workflows/scripts are configured in a single command. Also expands `setup-github` flags with `--dir`, `--beta-branch`, `--release-auth`, `--force`, and `--yes`.
