---
"@i-santos/ship": patch
---

Add layered ship configuration (`global`, repository, and repository-local overrides), introduce `ship config defaults` for managing release behavior preferences, and make release cleanup configurable across both `code` and `full` phases.
