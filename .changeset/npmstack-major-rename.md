---
'@i-santos/npmstack': major
---

Rename the CLI package from `@i-santos/npmstack` to `@i-santos/npmstack` and switch command usage to `npmstack` across docs, templates, and generated scripts. This is a breaking change in command and package identity.
