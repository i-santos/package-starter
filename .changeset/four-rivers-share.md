---
"@i-santos/create-package-starter": patch
---

Fix GitHub ruleset required status check context to use job name (`required-check`) instead of the full UI label, for both main and beta ruleset payloads.
