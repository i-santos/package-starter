{
  "name": "navy",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for the navy product development ecosystem",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test -w @i-santos/workflow && npm run test -w @i-santos/admiral && npm run test -w @i-santos/ship",
    "check": "npm run check -w @i-santos/workflow && npm run check -w @i-santos/admiral && npm run check -w @i-santos/ship",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run check && changeset publish",
    "beta:enter": "changeset pre enter beta",
    "beta:exit": "changeset pre exit",
    "beta:version": "changeset version",
    "beta:publish": "changeset publish",
    "beta:promote": "ship promote-stable --dir ."
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
