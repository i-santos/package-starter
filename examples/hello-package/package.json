{
  "name": "hello-package",
  "version": "0.1.2",
  "private": true,
  "description": "Pacote gerado pelo package-starter",
  "scripts": {
    "check": "node scripts/check.js",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run check && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
